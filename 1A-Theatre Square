#include<bits/stdc++.h>
using namespace std;
int main()
{
    long long int n,m,a,l,w;
    cin>>m>>n>>a;
    l=m/a;
    w=n/a;
    if(m%a!=0)
        l++;
    if(n%a!=0)
        w++;
    cout<<l*w<<endl;
    return 0;
}
