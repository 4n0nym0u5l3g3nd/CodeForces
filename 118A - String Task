#include<bits/stdc++.h>
using namespace std;
int main(){
    string a;
    int l,i;
    char b;
    cin>>a;
    l=a.length();
   for(i=0;i<l;i++){
    b=tolower(a[i]);
    if(b=='a'||b=='o'||b=='y'||b=='e'||b=='u'||b=='i')
        continue;
    else
        cout<<"."<<b;
   }
   return 0;
   
}
