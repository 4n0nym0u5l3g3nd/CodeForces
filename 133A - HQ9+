#include<bits/stdc++.h>
using namespace std;
int main(){
    string s;
    int l,i,a(0),c(0);
    cin>>s;
    l=s.length();
    for(i=0;i<l;i++){
        if(s[i]=='H' || s[i]=='Q' || s[i]=='9')
            c++;}
        if(c!=0)
            cout<<"YES"<<endl;
        else
            cout<<"NO"<<endl;
    return 0;
}
