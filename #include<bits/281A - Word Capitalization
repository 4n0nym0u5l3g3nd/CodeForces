#include<bits/stdc++.h>
using namespace std;
int main(){
    string a;
    int l,i;
    cin>>a;
    l=a.length();
    for(i=0;i<l;i++){
        if(a[0]>='a' && a[0]<='z')
        a[0]=a[0]-32;
    }
    cout<<a<<endl;
    return 0;
}
