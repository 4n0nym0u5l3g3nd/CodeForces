#include<bits/stdc++.h>
using namespace std;
int main(){
    string a;
    int r=0,n;
    cin>>n;
    while(n--){
    cin>>a;
    if(a=="X++" || a=="++X")
        r++;
    else if(a=="X--"||a=="--X")
        r--;}
    cout<<r<<endl;
    return 0;
}
